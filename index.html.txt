<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nitro 3D | Neon Drive</title>
    <style>
        body { margin: 0; overflow: hidden; background: #000; font-family: 'Segoe UI', sans-serif; }
        #ui { position: absolute; top: 20px; width: 100%; text-align: center; color: #00f3ff; pointer-events: none; z-index: 10; }
        .stats { font-size: 24px; text-shadow: 0 0 10px #00f3ff; font-weight: bold; }
        
        #menu { 
            position: absolute; inset: 0; background: rgba(0,0,0,0.85); 
            display: flex; flex-direction: column; justify-content: center; 
            align-items: center; z-index: 100; color: white;
        }
        button { 
            padding: 15px 50px; font-size: 1.5rem; background: #ff00ff; 
            color: white; border: none; border-radius: 50px; cursor: pointer;
            box-shadow: 0 0 20px #ff00ff; font-weight: bold; transition: 0.3s;
        }
        button:hover { transform: scale(1.1); filter: brightness(1.2); }

        .controls-hint { position: absolute; bottom: 30px; width: 100%; display: flex; justify-content: center; gap: 50px; }
        .btn { width: 80px; height: 80px; border: 2px solid #00f3ff; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: #00f3ff; font-size: 30px; touch-action: none; cursor: pointer; }
    </style>
</head>
<body>

<div id="ui">
    <div class="stats">SCORE: <span id="score">0</span></div>
</div>

<div id="menu">
    <h1 style="color:#00f3ff; letter-spacing: 8px; font-size: 3rem;">NITRO 3D</h1>
    <button onclick="startGame()">START RACE</button>
    <p style="margin-top:20px; color:#666; text-transform: uppercase;">Use Arrows or Touch Buttons</p>
</div>

<div class="controls-hint">
    <div class="btn" id="lBtn">◀</div>
    <div class="btn" id="rBtn">▶</div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

<script>
    let scene, camera, renderer, playerCar, road;
    let gameActive = false;
    let score = 0;
    let enemies = [];
    let speed = 1.0;
    let moveL = false, moveR = false;

    function init() {
        // 1. Setup Scene & Camera
        scene = new THREE.Scene();
        scene.background = new THREE.Color(0x050505);
        scene.fog = new THREE.Fog(0x050505, 10, 60); // Depth effect

        camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
        camera.position.set(0, 4, 10); // Third person follow view
        camera.lookAt(0, 1, -5);

        renderer
